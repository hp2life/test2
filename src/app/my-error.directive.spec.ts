import { MyErrorDirective } from './my-error.directive';

describe('MyErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new MyErrorDirective();
    expect(directive).toBeTruthy();
  });
});
